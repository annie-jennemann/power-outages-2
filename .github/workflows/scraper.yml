name: Run R Script

on:
  push:
    branches:
      - main
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  run-r-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libcurl4-openssl-dev \
            libssl-dev \
            libgit2-dev \
            libxml2-dev \
            libmagick++-dev \
            libharfbuzz-dev \
            libfribidi-dev \
            libfontconfig1-dev \
            pandoc \
            git \
            libudunits2-dev \
            libgdal-dev \
            libgeos-dev \
            libproj-dev \
            proj-data \
            proj-bin \
            gdal-bin

      - name: Cache R packages
        uses: actions/cache@v4
        with:
          path: ~/Rlib
          key: ${{ runner.os }}-R-${{ hashFiles('**/scripts/*.R') }}
          restore-keys: |
            ${{ runner.os }}-R-

      - name: Install R packages
        env:
          GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p ~/Rlib
          echo 'R_LIBS_USER=~/Rlib' >> ~/.Renviron
          echo 'options(repos = c(CRAN = "https://cloud.r-project.org"))' >> ~/.Rprofile

          Rscript -e '.libPaths("~/Rlib"); install.packages(c("fs","usethis","jsonlite","remotes","magick","sf","units","tidyverse"), dependencies = TRUE, lib="~/Rlib", repos="https://cloud.r-project.org")'

          Rscript -e '.libPaths("~/Rlib"); remotes::install_github("munichrocker/DatawRappr", dependencies = TRUE, upgrade = "never", lib="~/Rlib", auth_token=Sys.getenv("GITHUB_PAT"))'

      - name: Run R script
        env:
          AP_KEY: ${{ secrets.AP_KEY }}
          CHART_CODE: ${{ secrets.CHART_CODE }}
          URL: ${{ secrets.URL }}
        run: |
          . ~/.Renviron
          Rscript "scripts/power-outage-us-map.R"
